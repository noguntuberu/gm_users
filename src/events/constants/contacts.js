/** */

const batch_created = `bulk contacts created`;
const created = `contact created`;
const deleted = `contact deleted`;
const updated = `contact updated`;

module.exports = { batch_created, created, deleted, updated };